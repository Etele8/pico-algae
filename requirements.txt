# Non-PyTorch runtime dependencies for the Pico-Algae Counter UI.
#
# PyTorch (torch + torchvision) is installed SEPARATELY so you can pick a
# CPU or GPU (CUDA) build. Use Install.bat (interactive), or install manually:
#
#   CPU:       pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cpu
#   NVIDIA GPU: pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu126
#              (swap cu126 for cu128 or cu130 to match your NVIDIA driver)
#
# ...then:  pip install -r requirements.txt
# numpy is a floor (not pinned) so Python 3.14 works too: numpy 2.2.6 has no
# 3.14 wheels, but pip will pick a newer numpy (>=2.4.4) on 3.14 automatically.
flask==3.1.3
opencv-python==4.13.0.92
numpy>=2.2.6
pillow>=12.1.1
